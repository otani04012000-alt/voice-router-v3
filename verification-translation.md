# 翻訳王 検証記録 — 2026-09-12

## 検証した範囲

- `npm run build`：Next.js 16.3.4 / Turbopackの本番ビルドと型チェック成功。型エラー無視設定は削除。
- `npm test`：翻訳API 5ケース群、WebSocket結合テスト1ケース群成功。
- ブラウザ：Chromium、1440pxと390pxで描画を検査。横はみ出しなし。実行時例外なし。
- ブラウザの操作試験：翻訳、戻し訳、保存、保存後の再読み込み、拡大表示、180度回転、返事への切り替え、履歴保存の明示選択、API失敗時の原文保持。
- 2つのブラウザページから実WebSocketサーバーへ接続し、日本語→ベトナム語→日本語の中継・送信確認・ゲスト言語設定を検査。秘密の部屋を再読み込みすると履歴が残らないことを確認。
- WebSocket試験：原文と訳文の一致検証、不正な言語の拒否、別室への送信拒否、重複参加者IDの拒否、重複メッセージの抑制、入力中、退室。
- 既存の本番AI APIへ「こんにちは」を実送信し「Xin chào」が返ることを確認。既存Railwayの `/healthz` は正常。

## テストデータと実接続の区別

ブラウザ操作試験では、翻訳APIだけを固定応答へ差し替えています。UI・状態遷移・実WebSocket中継の試験であり、翻訳品質やAIの実接続検査ではありません。翻訳API単体試験でも上流を差し替え、エラーやUTF-8の分割を検査しています。

マイクを使った人間の発話認識、端末搭載音声による実際の音声出力はこの環境では実聴していません。ベトナム語・クメール語のネイティブ評価も未実施です。

## 再実行

```bash
npm ci
npm ci --prefix server
npm test
npm run typecheck
npm run build
# playwright-coreを別途インストールした環境でブラウザ操作試験
QA_NODE_MODULES=/path/to/node_modules QA_CHROMIUM=/path/to/chromium node tests/studio.e2e.cjs
```

ブラウザ試験は自身で3320番のアプリと3312番のWebSocketを起動し、終了時に停止します。画面キャプチャとランタイムログは `/tmp/studio-*` に出力します。
