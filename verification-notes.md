# Verification notes

- 本番URL `https://model-comparison-app-1.vercel.app/` は、確認時点で旧入口を表示していた。主文は「あなたへの専属コンシェルジュサポートルーム・秘密の部屋へようこそ。」で、導線は「部屋をつくる」とURL/コード入力の「入室する」だった。
- ローカル `http://localhost:3100/` は、変更後の確定仕様を表示した。見出しは「２人の専属オープンコンシェルジュサポート室」、ボタンは「いつものチャットを開始」と「秘密のチャットで開始」だった。
- ローカル入口の背景画像と霞・ハロー演出は既存のまま表示され、スマートフォン幅相当の画面でも二択が中央に収まっていた。
- 本番側にはまだ変更が反映されていない。GitHubへのコミット・pushとVercelのProduction反映が残っている。
- 通常チャットの最小ルートとして、ローカルには `/chat` を追加した。秘密側は既存の `/secret-room/{roomId}` を使う。

- ローカル入口の「いつものチャットを開始」は `/chat` へ遷移した。
- `/chat` には通常ルームの見出し、メッセージ入力欄、送信ボタンが表示された。
- 「こんにちは」を入力して送信すると、画面上にメッセージバブルが追加された。

- ローカル `/secret-room/test123` は404ではなく開き、`test123` が表示された。ただし現状はプレースホルダーで、翻訳入力や送信UIは未実装である。

- GitHub `main` へコミット `1d153ba` をpush後、本番URLを再確認した。入口は確定仕様へ反映済みで、見出しは「２人の専属オープンコンシェルジュサポート室」、ボタンは「いつものチャットを開始」「秘密のチャットで開始」と表示された。
- 本番入口はHTTP応答と画面表示を確認できた。Vercel MCPからはチーム配下のプロジェクト一覧が空で、既知スラッグの取得は404だったため、デプロイ詳細IDの取得まではできていない。ただし本番URLの実画面には変更が反映されている。

- 本番の「いつものチャットを開始」は `/chat` へ遷移し、通常ルームの入力欄と送信ボタンが表示された。
- 本番の `/secret-room/test123` は404ではなく開き、`test123` を表示した。ただし秘密側はまだプレースホルダーであり、実際の翻訳チャットUIは次の実装対象である。
